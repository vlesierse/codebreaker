import * as game from './game';

export const actionTypes = {
    game: game.actionTypes
}

export const actionCreators = {
    game: game.actionCreators
};

export const reducers = {
    game: game.reducer
};